<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:orientation="vertical">

    <TextView
        android:id="@+id/woohooTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="70dp"
        android:fontFamily="sans-serif-black"
        android:text="Welcome!"
        android:textAlignment="center"
        android:textColor="@color/blue_green"
        android:textSize="42sp"
        android:textStyle="normal" />

    <TextView
        android:id="@+id/allSetTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/woohooTextView"
        android:layout_marginTop="9dp"
        android:fontFamily="sans-serif-light"
        android:text="Please contact TabooKey\nto start using your safe"
        android:textAlignment="center"
        android:textColor="@color/text_color"
        android:textSize="18sp"
        android:textStyle="normal" />

    <Button
        android:id="@+id/sendEmailButton"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:layout_marginTop="50dp"
        android:paddingTop="95dp"
        android:layout_below="@+id/allSetTextView"
        android:background="@null"
        android:backgroundTint="@android:color/white"
        android:drawableTop="@drawable/send_email"
        android:text="support@tabookey.com"
        android:textAllCaps="false"
        android:textColor="@color/text_color"
        android:textSize="18sp" />

    <Button
        android:id="@+id/letsGoButton"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_alignParentBottom="true"
        android:background="@color/colorPrimary"
        android:minWidth="0dp"
        android:minHeight="0dp"
        android:padding="0dp"
        android:text="Start"
        android:textAlignment="center"
        android:textColor="@android:color/white"
        android:textSize="18sp" />
</RelativeLayout>